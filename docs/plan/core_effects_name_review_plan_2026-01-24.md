# core/effects のエフェクト名レビュー 計画（2026-01-24）

目的: `src/grafix/core/effects/` 配下の各エフェクト名（ファイル名・登録名）を棚卸しし、命名の一貫性/分かりやすさの観点でレビュー結果をドキュメント化する。

非目的:

- エフェクト名のリネーム実装（今回はレビューのみ）
- 挙動/アルゴリズムの改善

## 0) 確認（あなたの返答が必要）

- [x] “エフェクト名” の対象は **effect_registry の登録名**（= `@effect` の関数名、`E.<name>(...)` で使う名前）を主とし、補助的に **ファイル名** も見る；これでよい？
- [x] レビュー観点は「命名の一貫性」「意味の明確さ」「動詞/名詞の揃い」「略語の妥当性」「否定形/反転/強度系の表現」くらいに絞る；これでよい？
- [x] 出力先は `docs/review/core_effects_name_review_2026-01-24.md` とする；これでよい？

## 1) 手順

- [x] `src/grafix/core/effects/` のファイル一覧を取得する
- [x] `src/grafix/core/builtins.py` と各モジュールの `@effect` を確認し、対象エフェクト名（登録名）の一覧を確定する
- [x] 各エフェクトについて、次を 1 カードにまとめる
  - [x] 現在の名前（登録名 / ファイル名）
  - [x] “何をするエフェクトか” の 1 行要約（コード/ドックから）
  - [x] 命名レビュー（良い点 / 気になる点）
  - [x] 変更案（必要なら。複数案可）
- [x] レビュー結果を `docs/review/...` に新規 md として書き出す

## 2) 受け入れ条件（完了の定義）

- [x] `src/grafix/core/effects/` 配下の **全エフェクト** がレビュー文書に列挙されている
- [x] 少なくとも「命名の規則性」と「改善余地（あれば）」が各エフェクトで明示されている
