# 何を: `docs/review/api_param_order_2026-01-30.md` の冒頭に「優先修正タスク（ランキング）」チェックリストを追記する。

# なぜ: 情報量が多く、どこから直すべきかの意思決定が難しいため。

## 追記する場所（案）

- `## 共通パラメータ（順序ルール用）` の直後（= 一覧の前）に入れる。

## 出力フォーマット（案）

- 見出し: `## 優先修正タスク（ランキング）`
- チェックリスト形式（markdown）:
  - `- [ ] (P0) ...` のように優先度を明示
  - 可能なら `（破壊的）` を付ける
  - 影響範囲（対象 op）を 1 行で併記する

## ランキング方針（案）

- P0: 影響範囲が広く、かつ “順序ルール” の前提になる語彙（中心・平行移動・位相・頻度など）
- P1: 混同が起きやすい命名衝突（例: `keep_*` の衝突）
- P2: UI/順序の改善（破壊的ではないが一貫性が上がるもの）

## タスク候補（案）

（この候補を md にチェックリストとして書く）

- (P0) vec3 平行移動の正規名を決めて統一（例: `delta` に寄せる）
  - 対象例: `E.repeat offset -> delta`, `P.layout_* offset -> delta`
- (P0) ノイズ系の語彙統一（`frequency` / `phase`）
  - 対象例: `E.displace spatial_freq -> frequency`, `E.displace t -> phase`
- (P1) 中心座標の語彙統一（2D 分割名）
  - 対象例: `E.mirror cx/cy -> center_x/center_y`
- (P1) `keep_*` 衝突の解消
  - 対象例: `E.drop keep_mode -> mode`
- (P2) `seed` の配置ルールを決めて揃える（末尾寄せ等）
  - 対象例: `E.partition`, `E.bold`, `E.drop`, `E.reaction_diffusion`
- (P2) トグル → 依存パラメータの順を揃える
  - 対象例: `P.layout_bounds show_trim/trim`

## 実装チェックリスト

- [x] 1) 上の候補と優先度で良いか確認する（増減/順序）
- [x] 2) `docs/review/api_param_order_2026-01-30.md` にランキング・チェックリストを追記
- [x] 3) 一覧が読みやすくなったかを見直す（長すぎたら上位 5 件に絞る）
