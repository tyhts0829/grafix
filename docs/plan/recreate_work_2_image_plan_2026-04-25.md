# sketch/work/2.py 画像再現計画

対象: `sketch/work/2.py`

## 目的

添付画像の構成を Grafix のスケッチとして再現する。

## 作業項目

- [x] 既存のスケッチ API と `sketch/work` 周辺の書き方を確認する。
- [x] 画像を構成要素に分解する。
  - クリーム色の背景
  - 黒い太い連結形状
  - 右上の赤橙円
  - 下部の楕円影
  - 下側の白い有機形状内に流線模様
- [x] `sketch/work/2.py` に、画像再現用の `draw()` を実装する。
- [x] ローカルレンダリングで構図と実行可否を確認する。
- [x] 必要に応じて線密度・形状比率・色を微調整する。
- [x] 完了項目と未完了項目をこの計画に反映する。

## 方針

- 依頼対象の `sketch/work/2.py` だけを編集する。
- `sketch/work/1.py`、`.vscode/`、`sketch/work/2.PNG` など依頼範囲外の差分には触れない。
- 互換ラッパーや追加抽象は作らず、単体スケッチとして読みやすく保つ。
