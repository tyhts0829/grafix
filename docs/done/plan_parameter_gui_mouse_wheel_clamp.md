# parameter_gui: マウスホイールΔをクランプして暴れを抑える（実装計画）

目的
- parameter_gui のホイールスクロールが入力デバイス（特にトラックパッド）によって極端に大きく入り、調整しにくい問題を緩和する。

方針
- `imgui.get_io().mouse_wheel` の「このフレームのホイールΔ」を `[-1.0, 1.0]` にクランプする。
- 既存の「スクロール方向反転」は維持する。
- 互換ラッパーや設定追加は行わず、まず最小変更で体感を改善する。

変更箇所
- `src/grafix/interactive/parameter_gui/gui.py`

手順
- [ ] 変更前の挙動を確認（ホイールΔが大きい環境で再現）
- [x] `ParameterGUI.draw_frame()` 内で `io.mouse_wheel` を符号反転した後、`[-1.0, 1.0]` にクランプする
- [ ] 実機でスクロール体感を確認（特にトラックパッド/マウスでの差）
- [ ] 問題なければこの計画を完了チェックする
