# 何を: `docs/review/api_param_order_2026-01-30.md` の冒頭に「共通パラメータ」セクションを追加する。

# なぜ: 命名/順序ルールを決める前に、まず “横断でよく出る語彙” を固定して比較しやすくするため。

## 追加する内容（案）

- 見出し: `## 共通パラメータ（順序ルール用）`
- 箇条書きで、以下の「よく出る語彙」を列挙する（必要なら `A/B` のように正規名を明示）。

### 候補（案）

- `center`
- `scale`
- `rotation` / `angle`（vec3 とスカラーの区別を含めて方針化）
- `delta`（平行移動 vec3）
  - 現状の `offset` は用途が混在するので、**空間平行移動**は `delta` に寄せる、など
- `auto_center` / `pivot`（transform の pivot 指定）
- `seed`
- `phase`（位相/時間。`t` のような別名は `phase` へ寄せる、など）
- `mode`（動作モード）

## 要確認

1) 「共通」として列挙する基準:
   - A: primitive/effect/preset の “どれか 2 系統以上” で出る語彙だけに絞る
   - B: effect 内だけでも頻出なら含める（例: `auto_center/pivot`）
   どちらが良い？: B

## 実装チェックリスト

- [x] 1) 上の「共通の基準」を合意する
- [x] 2) `docs/review/api_param_order_2026-01-30.md` の冒頭へセクションを追加する
- [x] 3) 箇条書きが “順序ルールの材料” として過不足ないか見直す
